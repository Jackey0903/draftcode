# 卡片悬浮微动

## 适合

流程节点、竞品卡、能力卡、列表项、玻璃卡。

## 效果

hover 时卡片上浮 1-4px,边框或顶高光增强,图像可轻微 scale。

## 实现抓手

- `transform: translateY(-1px 至 -4px)`。
- `border-color` 提亮。
- 可加 `box-shadow` 或 `::before` 顶高光。
- duration 200-350ms。

## 禁忌

- 不要把卡片浮太高。
- 不要在触摸屏上只依赖 hover。
- 不要让 hover 改变布局尺寸。
